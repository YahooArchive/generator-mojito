YUI.add('<%= module %>', function (Y, NAME) {
    'use strict';

    Y.namespace('mojito.models')[NAME] = {

        init: function (config) {

        }

    };

}, '0.0.1', {
    requires: [
    ]
});
