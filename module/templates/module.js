YUI.add('<%= module %>', function (Y, NAME) {
    'use strict';

    function <%= Base %>() {

    }

    <%= Base %>.prototype = {

    };

    Y.<%= Base %> = <%= Base %>;

}, '0.0.1', {
    requires: [
    ]
});